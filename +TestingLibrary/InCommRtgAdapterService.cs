namespace TestingLibrary
{
    class InCommRtgAdapterService
    {
        /*
        public bool BalanceTransfer(IRequest request, IResponse response)
        {
            if (request == null) throw new ArgumentNullException("request");
            if (response == null) throw new ArgumentNullException("response");

            // TODO: The GetBalance logic must be moved to RTG 2.0 once they support BalanceInquiry!!!
            if (request.Amount.IsDecimalValueZero())
                request.Amount = GetBalance(request, request.TransferSourceCardNumber, null).ToString(CultureInfo.InvariantCulture);

            return Rtg2AdapterService.BalanceTransfer(request, response);
        }
        */

    }
}
